# Base URL of the backend API. Leave unset in dev (the Vite dev server proxies
# /api to http://localhost:3001, see vite.config.ts). Set this to the deployed
# backend's URL when building for production, e.g. https://api.example.com/api
VITE_API_BASE_URL=
